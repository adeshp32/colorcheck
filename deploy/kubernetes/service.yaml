apiVersion: v1
kind: Service
metadata:
  name: colorcheck
  labels:
    app: colorcheck
spec:
  selector:
    app: colorcheck
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
